# App Template Instructions

- Each app directory contains source files for one generated `.app` bundle.
- Commit icons, scripts, and plists; do not commit generated `.app` bundles.
- Avoid hardcoded private hostnames, user paths, or credentials.
