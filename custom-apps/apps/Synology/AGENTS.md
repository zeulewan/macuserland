# Synology Instructions

- Keep NAS host and share values in local config, not Git.
- Default config path: `~/.config/macuserland/synology.conf`.
- The app should fail with a notification when config is missing.
